# Env map — Employee 01

| Variable | Purpose | Where |
|----------|---------|-------|
| `OBSIDIAN_VAULT_PATH` | Vault root | `~/.hermes/profiles/bej/.env` |
| `WIKI_PATH` | Wiki skill | same |
| API keys | LLM, Discord, Himalaya | `~/.hermes/profiles/bej/.env.encrypted` only |

**Never** store secrets in this control room or the git repo.

`terminal.cwd` in profile config → bejcapital repo (code tools).
