# Runbook — Employee 03

Manual: `portfolio review` in `hermes -p bej`.

Cron enable after shadow graduation:

```bash
hermes -p bej cron resume <portfolio-review-job-id>
```

Outputs: `05-HERMES/agents/employee-03-portfolio-manager/portfolio-reviews/`.
