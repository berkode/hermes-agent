# Runbook — Orchestrator

```bash
hermes kanban create berkode-ops --description "Berkode employee board"   # once
hermes -p ops chat
```

Ask: "Route this to the right desk" — uses Kanban decompose.

Ensure gateway running for dispatcher. See `shared/assignee-map.md`.
