# Env map — Employee 04

| Variable | Profile |
|----------|---------|
| `terminal.cwd` | bejcapital (via `ops` config) |
| Secrets | `~/.hermes/profiles/ops/.env.encrypted` |

Merge Kanban orchestrator block from `shared/ops-kanban-config.example.yaml` when using `ops` as fleet orchestrator.
